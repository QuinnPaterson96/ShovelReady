# Next steps after wave ten

## Immediate: SR-41 / issue #89

Mount the reviewed SitePreparation and ModelInputs components in the current Civic
Atlas assessment. Keep source and manual values together with their full provenance;
plain form strings are insufficient. Every change must invalidate prior summaries,
including site/model switches and changes to height reference. Explicitly confirm a
parcel candidate. A manual address alone is not a verified parcel identity.

Replace unconditional warnings with per-field/per-check readiness. Show the exact
three-PID coverage and unavailable address join honestly. Do not show model service
availability as confirmed: Landing is excluded by the captured provider statement;
aux box delivery claims do not establish installation service. Height remains unknown
unless a value and reference are supplied. These candidates do not yet support fit.

Display the Victoria cited rule candidates as evidence, with review/currentness gaps;
keep evaluation disabled until actual acceptance and input requirements are met.
Add timeout/cancellation and unmount behavior to the standalone site fetch before
mounting it. Validate user-entered area visibly instead of silently treating invalid
input as unknown. Preserve attributions and source dates through the summary.

Verify the mounted producer-to-browser flow and manual fallback, malformed/unavailable
responses, ambiguity, changed inputs and stale requests. Use one real retained PID/model
preparation plus an unresolved case. Then run a bounded browser-only virtual-user
rehearsal; adjudicate its findings. Do not describe this as legal/source review.

## Parallel evidence work, only where it resolves a named blocker

- SR-38/#86: acquire and verify a permitted Victoria address-to-PID join with retained
  samples. Current endpoint intentionally returns unavailable for addresses. Reuse
  the existing source/repository pipeline; no speculative citywide infrastructure.
- SR-05/06/10: review one current pathway and controlled model/site/placement subset.
  Resolve the recorded bylaw consolidation/amendment discrepancy and measurement
  definitions. A source hash/quotation check cannot supply independent acceptance.
- Confirm one provider's actual Victoria service and controlled dimensions before
  presenting a purchasable local match. Provider outreach requires explicit direction.

## Expansion decision

Saanich has three reproducible address/parcel candidates and licensed source excerpts,
but invalid zoning geometry needs diagnosis against original source rings/conversion.
Do not auto-repair it into legal coverage. Langford has licensed provincial captures,
but all three geocoder responses are ambiguous; explicit candidate selection is needed.
Municipal zoning/building/constraint reuse rights remain unresolved.

Keep both as offline transfer experiments until those concrete gaps are addressed.
Share the model catalogue. No new municipal runtime framework or additional broad
research reports are required before the Victoria workflow is usable.

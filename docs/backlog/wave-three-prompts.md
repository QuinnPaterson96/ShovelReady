# Wave three: evaluator core and real spatial evidence viewer

**Completed implementation wave:** PRs #31 (evaluator), #32 (local database) and #33
(observation API/UI) are merged. The prompts below are historical launch instructions,
not tasks to dispatch again. See the [integration review](../integration-review-2026-09-24.md)
for the current verification and remaining gates; the parent MVP tickets are not
completed by these partial deliveries.

Checkpoint: September 24, 2026, after PRs [#28](https://github.com/QuinnPaterson96/ShovelReady/pull/28) and [#29](https://github.com/QuinnPaterson96/ShovelReady/pull/29). This supersedes the launch instructions in [wave two](wave-two-prompts.md). GitHub issues remain the live status source. These two tasks are partial deliveries toward existing tickets, not a claim that M1 is complete.

## Integration evidence and limits

- SR-07 now has offline saved-response replay, conservative normalization and explicit benchmark/context eligibility gates. All 25 real corpus entries remain ineligible; accuracy, live cost and human effort remain unmeasured. Review fixed invalid field names losing the diagnostic bundle and unresolved auxiliary objects returning CLI success. Four regression cases were added.
- SR-09 imports 15 licensed source records and nine feature responses, retaining ten source-scoped features and three parcel analyses. It preserves raw XYZ, source identity, CRS and attribution. Migration 0003 admits a versioned spatial observation payload. Review corrected physical overlap area where three zones coincide; all intersections remain retained.
- Combined local verification passed: 119 pytest cases and 28 contract subtests using a new disposable PostgreSQL 17 cluster; full backend lint; two acquisition, ten intake and three preview checks. Required backend, frontend and container-startup checks passed before both PRs merged. The pre-existing Starlette/httpx deprecation warning remains.
- No accepted zoning dataset, real design/site/placement revision, working evaluator, publication, cloud deployment or live extraction score was created. Geometry calculations do not establish survey accuracy, legal site boundaries or zoning applicability. SR-07 and SR-09 remain open.

Shapely/pyproj satisfy the bounded batch operations; PostGIS is deferred until database spatial queries or measured scale require it. Address resolution is also deferred for the sample-selection workflow: SR-09's address-join criterion remains explicitly unfulfilled, not silently checked off. The shared SR-04 geometry model still cannot substitute for arbitrary raw Esri features.

## Parallel ownership and sequencing

| Task | Deliver now | Owns | Still gated |
|---|---|---|---|
| F3 / SR-10, issue #10 | Deterministic scalar-check core and conservative aggregation, tested with labelled synthetic rules/facts | `app/evaluation/`, focused evaluator tests/fixtures, `docs/evaluation.md`; narrowly justified shared contract changes | Real clause interpretation, reviewed measurements/placement, 5–8 actual pilot checks and real-site outcomes |
| G3 / SR-12, issue #12 | Read-only investigation of the existing licensed spatial import through FastAPI and React | API module and `app/main.py`, frontend, API tests, `docs/investigation-api.md`; shared tooling/CI/config if needed | Screening endpoints, accepted release selection, evaluated design/placement results |

Both start from merged main in separate managed worktrees. Neither waits for the other. G3 consumes the existing `SpatialImport` and `SourceSnapshot` interfaces and must not call a speculative evaluator API. F3 owns any evidence-driven scalar/result contract proposal; G3 does not change those shared contracts. Keep API DTOs local to the investigation module. Neither task should need persistence migrations or ingestion changes. If an actual shared change is necessary, document it and coordinate before editing the other track's files.

G3 owns shared dependencies/lockfiles/CI for this wave; prefer existing packages. F3 uses current Python/Pydantic/Decimal tooling. Preserve all existing checks, including guarded PostgreSQL migrations and the original synthetic preview. Each task writes its focused handoff documentation; central README/backlog/architecture reconciliation belongs to integration to reduce overlapping edits.

Use the managed worktree attached to each task; do not create another or touch the original dirty checkout. Verify the branch contains merged PRs #28/#29 and this handoff. Create the stated `codex/` branch only after checking current status. Read AGENTS.md and verify both effective author/committer and personal Git/GitHub authentication. The configured personal CLI directory is `C:/Users/quinn/.config/gh-quinnpaterson96`; using it is not a substitute for verifying the authenticated account. Never print tokens or use a work identity. Commit explicit paths, push and open a PR against main for review; do not auto-merge or close the parent issue on partial delivery.

No new paid resources, live model runs, source recapture, outreach, agents, universal rule language, automatic layout search or cloud deployment is part of this wave. Ordinary CI is offline. Use only disposable test databases and explicit ShovelReady configuration; no Styx or legacy database fallback. Authorization for code work does not supply missing source rights or reviewer approval.

## Prompt F3: SR-10 bounded evaluator core

```text
Implement the bounded evaluator-core portion of ShovelReady SR-10 / GitHub issue #10 in this task's managed worktree, on codex/bounded-evaluator. Do not create another worktree. Read AGENTS.md, README.md, docs/architecture.md, docs/quality.md, the live issue and docs/backlog/wave-three-prompts.md. Confirm your starting commit contains merged PRs #28/#29 and the wave-three handoff; do not reset unrelated changes.

Inspect app/contracts, app/persistence, docs/data-contracts.md, docs/extraction.md, docs/spatial.md and docs/pilot-inputs/reviewer-actions.md. There are no accepted real clause/site/placement oracle cases. The 25 provisional corpus entries and the preview's asserted results cannot become accepted rules or tests of legal accuracy. No real fit should be fabricated.

Build ordinary deterministic Python functions that actually evaluate the current supported scalar-bound semantics against explicitly bound, reviewed measurements, then aggregate checks within coherent alternatives. Reuse the existing quantity, rule and result contracts where their meanings fit. A small versioned evaluator input/diagnostic envelope is acceptable for concrete missing information, such as fact binding and trace, but not a new universal rule language. Start with a few representative operations, not a promise to implement 5–8 real zoning checks without evidence. Do not calculate roof/grade, floor-area inclusions, setbacks or legal yards from incomplete GIS/manufacturer data.

Require exact rule revisions, input revisions, measurement definitions, compatible units and ratio numerator/denominator, applicable scope and evidence. A reviewed label alone does not make a free-text condition executable. Unreviewed facts/applicability, unsupported definitions, unresolved references, conditions/exceptions or unsupported override/calculation relationships must remain explicit investigation states. Even resolved references are not permission to ignore their semantics. Do not choose a measurement by matching its name or dimension alone. Unknown/missing never becomes zero or unlimited. Preserve approval state separately from numeric outcomes and never infer as-of-right approval from scalar passes.

Compute every declared alternative without combining favourable bounds between them. Preserve unresolved alternatives. A failure applies only to the supplied placement and evaluated pathway; outside coverage is investigation, not prohibition. Revalidate boundary payloads and version compatibility, including constructed/copied models. Preserve the facts used, evidence, versions, exclusions and why a check could not run; do not overload a published-release label to claim draft data was activated. Keep this core independent of HTTP, database writes and publication. The existing repository stores draft manifests; this task does not create an active release.

Use clearly labelled synthetic fixtures with independently calculated expected outcomes. Cover exact threshold inclusivity, equivalent units, 45 percent versus fraction 0.45 and FSR 1.2 with explicit bases, wrong definitions/bases, missing/unreviewed inputs, unsupported conditional and referenced rules, incompatible alternatives, ordering invariance, outside coverage, unknown approval and failed-placement overgeneralization. Test that the current real packet is refused or reported as not ready, without manufacturing Evidence or input revisions merely to make validation pass. Distinguish synthetic decision tests from source-support/legal accuracy.

Own app/evaluation, focused evaluator tests/fixtures and docs/evaluation.md. You own narrowly justified changes to shared scalar/result contracts only if current shapes demonstrably cannot preserve required meaning; document compatibility and keep existing payload readers/tests working, or explicitly version the new boundary. G3 independently owns the real spatial investigation API/UI and shared tooling/CI. Do not edit its modules, ingestion/spatial import, persistence migrations, frontend or central backlog. Finish with a callable interface, actual evidence, concrete shared-contract gaps and exact reviewed-data gates for later integration.

Run meaningful offline checks and the guarded disposable PostgreSQL suite if shared contracts are changed. Verify QuinnPaterson96 author/committer and personal Git/GitHub authentication. Commit explicit paths, push and open a PR against main without merging. Keep issue #10 open: this is technical core preparation, not completion of real supported pilot checks. Return the PR, interface, verification and remaining gates.
```

## Prompt G3: SR-12 real spatial investigation API and UI

```text
Implement the real spatial-evidence investigation slice of ShovelReady SR-12 / GitHub issue #12 in this task's managed worktree, on codex/spatial-investigation-ui. Do not create another worktree. Read AGENTS.md, README.md, docs/architecture.md, docs/quality.md, the live issue and docs/backlog/wave-three-prompts.md. Confirm your starting commit contains merged PRs #28/#29 and this handoff; preserve unrelated work.

Inspect app/main.py, app/persistence, app/spatial, docs/spatial.md, docs/persistence.md, the licensed intake packet and the synthetic frontend preview. SR-09 can explicitly import the licensed packet into PostgreSQL: 15 sources, nine responses, ten source-scoped features and three parcel analyses. These are unreviewed observations, not legal site facts or evaluated fit. There is no accepted dataset, fixed design revision, supplied real placement or active publication. Do not wait for F3 or invent its evaluator interface.

Expose a small typed, versioned read-only API for an explicitly selected stored spatial-import revision and its licensed source metadata. Use the existing Repository read interface; do not duplicate persistence or select the latest revision as an implicit active release. Retain source/layer/request identities, source date, hash, original feature locator, CRS, attribution, geometry issues and query-scoped intersections. Restrict the investigation surface to the configured licensed pilot collection and its pinned source records. Do not expose a generic record/file browser, arbitrary artifact URI reader, private paths/bylaw/manufacturer bytes, runtime recapture or public writes. Source-derived text is inert content; validate links and URLs before rendering them.

Add a clearly labelled real-observations mode to the thin React UI, separate from the existing fictional preview. Select captured sample leads/features by their scoped IDs, inspect parcel/roofline/zoning observations, all intersections/slivers/boundary touches and missing facts. Say that no zoning screening has been performed; all three remain investigation cases. No green buildable labels or invented rule citations, source acceptance, design fit or release IDs. Preserve the synthetic preview for its existing scenarios and label it consistently. Loading, unavailable configuration/database, missing revision and invalid responses must show honest states, never silent fixture fallback.

A simple local SVG view of the original EPSG:3157 XY geometry is sufficient; preserve holes/multiple parts and distinguish rooflines from wall footprints or principal-building identity. No map-provider account or tile dependency is necessary. The derived longitude/latitude in SR-09 is EPSG:4617, not automatically RFC 7946/WGS84 GeoJSON: do not relabel it EPSG:4326. Display geometric area as geometric, not regulatory lot area. Do not infer legal yards, grade or placement from the map.

Require explicit ShovelReady database/revision configuration. HTTP startup and reads must not migrate, import or publish; health remains process liveness. Provide a reproducible operator sequence using existing explicit migration/import commands and a disposable local database. No cloud configuration or legacy DATABASE_URL fallback. API failures must not expose connection strings or private artifact paths. Keep the reader narrow and source evidence accessible without bypassing SR-11's acceptance/publication gate for future screening.

Own a focused investigation API module/DTOs, app/main.py integration, frontend, focused API/UI tests and docs/investigation-api.md. You own shared dependencies/lockfiles/CI/config if actually needed; extend existing jobs and preserve offline extraction, intake, preview, spatial and disposable PostgreSQL checks. F3 owns app/evaluation and any scalar/result contract proposals. Consume current SourceSnapshot and SpatialImport without altering them or persistence migrations. Put interface/demo instructions in your focused document; central README/backlog reconciliation belongs to integration.

Verify the real licensed import -> database -> API -> UI path, unknown/no-data states, pinned identities and source restrictions, malformed/versioned API payload handling, inert source text and meaningful map controls. Use the guarded disposable database runner and add relevant API tests, frontend typecheck/build and a browser walkthrough. Reuse the existing task rubric for an author rehearsal and record observations as simulated, not independent user evidence. Do not claim screening or actual customer validation.

Verify QuinnPaterson96 author/committer and personal Git/GitHub authentication. Commit explicit paths, push and open a PR against main without merging. Keep issue #12 open for screening/release/evaluator integration. Return the PR, commands to see the real observations, tests/browser evidence, data-access constraints and exact remaining gates.
```

## Next integration and owner inputs

Review these PRs together but merge independently when ready. First verify F3's semantics and refusal cases; verify G3 shows the actual stored licensed observations and no fabricated evaluated result. Preserve existing contract readers and data identities. Then scope SR-11 publication and SR-12 screening integration around the actual reviewed subset and callable evaluator; SR-13 must verify the complete saved-source-to-result path before hosting/CD in SR-14/15. Do not introduce an accepted dataset by selecting an observational revision.

The critical external path is unchanged: [reviewer actions](../pilot-inputs/reviewer-actions.md) names the controlled manufacturer configuration/drawings, authorized durable source/excerpt retention, independent rule/applicability reviewer, and reviewed site facts plus supplied placements. Start with a genuinely reviewable small subset; do not require completing all 25 clauses before finding the first supported checks, and do not confuse a small subset with complete zoning coverage. No additional broad research is necessary to launch this wave. SR-16 remains real customer testing after a useful prototype; SR-17/18 remain deferred.
